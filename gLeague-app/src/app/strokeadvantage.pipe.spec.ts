import { StrokeadvantagePipe } from './strokeadvantage.pipe';

describe('StrokeadvantagePipe', () => {
  it('create an instance', () => {
    const pipe = new StrokeadvantagePipe();
    expect(pipe).toBeTruthy();
  });
});
